fastapi==0.111.0
uvicorn==0.30.1
sqlalchemy==2.0.31
pydantic==2.7.1
pydantic-settings==2.2.1

# DB driver (SQLite is built-in, add others if needed)
# psycopg2-binary==2.9.9   # (only if you use Postgres)

# Auth & Security
python-jose[cryptography]==3.3.0
passlib==1.7.4

# HTTP requests
httpx==0.27.0

# Utils
cachetools==5.3.3
passlib[argon2]
argon2_cffi    
